import * as React from "react";

export class Footer extends React.Component{
    render(): React.ReactNode {
        return (
            <div>
                footer
            </div>
        );
    }
}
