import * as React from "react";

export class Header extends React.Component{
    render(): React.ReactNode {
        return (
            <div>
                header
            </div>
        );
    }
}
